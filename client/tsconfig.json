{
  "compilerOptions": {
    "lib": ["DOM", "ESNext"],
    "target": "ESNext",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "esModuleInterop": true,
    "outDir": "./dist",
    "newLine": "lf",
    "allowSyntheticDefaultImports": true,
    "useDefineForClassFields": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "allowImportingTsExtensions": true,
    "allowJs": true,
    "strict": true,
    "skipLibCheck": true,
    "types": ["vite/client", "turnstile-types"]
  },
  // Specifies an array of file paths or patterns to include in the compilation process.
  // - "src/**/*.ts": Includes all TypeScript files in the 'src' directory and its subdirectories.
  // - "src/**/*.js": Includes all JavaScript files in the 'src' directory and its subdirectories.
  // - "../shared/": Includes all files in the 'shared' directory located one level up from the current directory.
  // - "atlas-builder/**/*.ts": Includes all TypeScript files in the 'atlas-builder' directory and its subdirectories.
  // - "global.d.ts": Includes the 'global.d.ts' file, typically used for global type declarations.
  "include": [
    "src/**/*.ts",
    "src/**/*.js",
    "../shared/",
    "atlas-builder/**/*.ts",
    "global.d.ts"
  ]
}
